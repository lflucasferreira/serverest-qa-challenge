{
  "type": "object",
  "properties": {
    "message": { "type": "string" },
    "authorization": { "type": "string", "pattern": "^Bearer .+" }
  },
  "required": ["message", "authorization"],
  "additionalProperties": false
}
